<section>
  <h2><%= book.title %></h3>
    <h3><%= book.author %></h3>
    <h3><%= book.bookshelf %></h3>
    <p><%= book.description %></p>
    <p><%= book.isbn %></p>
    <img src="<%= book.image_url %>" alt="<%= book.title %>">
</section>